spring:
  application:
    name: indica-ai-let-parque
  datasource:
    url: jdbc:h2:mem:indicaai
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
